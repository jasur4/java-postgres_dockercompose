# java-postgres_dockercompose

Docker compose builds and run Java app on Spring boot + Postgresql database. Web site on Java shows current time on Postgresql.

Download project and run command:

docker compose up --build -d

Then, enter the website:

http://localhost:8080
